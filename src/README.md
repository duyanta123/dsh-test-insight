# Core modules

- `input.mjs` normalizes all/change/target inputs and the three scanner Git channels.
- `errors.mjs` defines stable error codes and exit codes.
- `redaction.mjs` provides the first report/command redaction layer.
- `index.mjs` is the shared public core used by the CLI, plugin and future report pipeline.
